import { NextRequest, NextResponse } from "next/server"; import { compare } from "bcryptjs"; import { createSession, sessionCookie } from "@/lib/auth";
export async function POST(req:NextRequest){const form=await req.formData();const email=String(form.get("email")||"");const password=String(form.get("password")||"");const expected=process.env.ADMIN_EMAIL;const hash=process.env.ADMIN_PASSWORD_HASH;const valid=Boolean(expected&&hash&&email.toLowerCase()===expected.toLowerCase()&&await compare(password,hash!));if(!valid)return NextResponse.redirect(new URL("/admin/login?error=1",req.url),303);const res=NextResponse.redirect(new URL("/admin",req.url),303);res.cookies.set(sessionCookie,await createSession(),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",maxAge:60*60*8,path:"/"});return res}
