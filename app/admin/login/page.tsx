import "../admin.css";
export default function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { return <main className="login-shell"><form className="login-card" action="/api/auth/login" method="post"><p className="admin-kicker">Atelier Service</p><h1>Admin sign in</h1><p>Manage website content, designs, media and client enquiries.</p><label>Email<input required name="email" type="email" autoComplete="email"/></label><label>Password<input required name="password" type="password" autoComplete="current-password"/></label><button type="submit">Sign in</button><ErrorMessage searchParams={searchParams}/></form></main>; }
async function ErrorMessage({searchParams}:{searchParams:Promise<{error?:string}>}) { const p=await searchParams; return p.error?<p className="form-error">Invalid email or password.</p>:null; }
