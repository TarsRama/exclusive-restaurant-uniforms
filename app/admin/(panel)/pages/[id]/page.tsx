import Link from "next/link";import { asc,eq } from "drizzle-orm";import { notFound } from "next/navigation";import { db } from "@/lib/db";import { pages,sections } from "@/db/schema";import { sectionLabels,sectionTypes,type SectionType } from "@/lib/page-builder";import { Header } from "../../components";
const Hidden=({name,value}:{name:string,value:string})=><input type="hidden" name={name} value={value}/>;
export default async function PageEditor({params}:{params:Promise<{id:string}>}){const {id}=await params;if(!db)return null;const [page]=await db.select().from(pages).where(eq(pages.id,id)).limit(1);if(!page)notFound();const rows=await db.select().from(sections).where(eq(sections.pageId,id)).orderBy(asc(sections.sortOrder));return <><Header title={page.title} text="Edit settings and compose the page from reusable sections." action={<div className="header-actions"><Link href={page.slug?`/${page.slug}`:"/"} target="_blank">Preview ↗</Link>{page.slug&&<Action action="delete-page" id={page.id} label="Delete page" danger/>}</div>}/><form className="admin-card editor-form" action="/api/admin/page-builder" method="post"><Hidden name="action" value="update-page"/><Hidden name="id" value={page.id}/><div className="form-grid"><label>Title<input name="title" defaultValue={page.title} required/></label><label>URL slug<input name="slug" defaultValue={page.slug}/></label><label>Navigation label<input name="navLabel" defaultValue={page.navLabel}/></label><label>Navigation order<input type="number" name="navOrder" defaultValue={page.navOrder}/></label><label className="wide">SEO title<input name="metaTitle" defaultValue={page.metaTitle}/></label><label className="wide">SEO description<textarea name="metaDescription" defaultValue={page.metaDescription}/></label><label className="check"><input type="checkbox" name="showInNav" defaultChecked={page.showInNav}/> Show in navigation</label><label className="check"><input type="checkbox" name="published" defaultChecked={page.published}/> Published</label></div><div className="form-actions"><span>Page settings and SEO</span><button>Save page</button></div></form><section className="admin-card"><div className="card-heading"><h2>Page sections</h2><span>Rendered top to bottom</span></div><div className="builder-list">{rows.map((s,i)=><article key={s.id}><span className="order-number">{i+1}</span><div><strong>{sectionLabels[s.type as SectionType]??s.type}</strong><span>{s.visible?"Visible":"Hidden"}</span></div><div className="row-actions"><Move id={s.id} direction="up" disabled={i===0}/><Move id={s.id} direction="down" disabled={i===rows.length-1}/><Link href={`/admin/sections/${s.id}`}>Edit →</Link><Action action="duplicate-section" id={s.id} label="Copy"/><Action action="delete-section" id={s.id} label="Delete" danger/></div></article>)}</div><form className="add-section" action="/api/admin/page-builder" method="post"><Hidden name="action" value="add-section"/><Hidden name="pageId" value={page.id}/><select name="type">{sectionTypes.map(t=><option key={t} value={t}>{sectionLabels[t]}</option>)}</select><button>Add section</button></form></section></>}
function Action({action,id,label,danger=false}:{action:string;id:string;label:string;danger?:boolean}){return <form action="/api/admin/page-builder" method="post"><Hidden name="action" value={action}/><Hidden name="id" value={id}/><button className={danger?"danger-button":""}>{label}</button></form>};function Move({id,direction,disabled}:{id:string;direction:string;disabled:boolean}){return <form action="/api/admin/page-builder" method="post"><Hidden name="action" value="move-section"/><Hidden name="id" value={id}/><Hidden name="direction" value={direction}/><button disabled={disabled}>{direction==="up"?"↑":"↓"}</button></form>}
